export function evaluate({tasks=[],overloadThreshold=10}={}){const loads={};for(const t of tasks){if(t.completed)continue;loads[t.owner||'unassigned']=(loads[t.owner||'unassigned']||0)+Number(t.weight||1)}const overloaded=Object.entries(loads).filter(([,v])=>v>overloadThreshold).map(([owner,load])=>({owner,load}));return{ok:true,loads,overloaded};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}