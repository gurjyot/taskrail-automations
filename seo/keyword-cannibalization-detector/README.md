# Keyword Cannibalization Detector

Groups Search Console query/page rows and flags queries where multiple URLs receive meaningful impressions, especially when rankings are close enough to indicate competing intent.
