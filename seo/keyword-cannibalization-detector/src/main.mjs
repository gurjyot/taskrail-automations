export function evaluate({rows=[],minImpressions=50}){const by={};for(const r of rows){if(Number(r.impressions||0)<minImpressions)continue;(by[r.query]??=[]).push(r)}const issues=[];for(const[q,pages]of Object.entries(by)){const urls=[...new Set(pages.map(x=>x.page))];if(urls.length<2)continue;const ranked=pages.sort((a,b)=>Number(a.position||99)-Number(b.position||99));if(Math.abs(Number(ranked[0].position||99)-Number(ranked[1].position||99))<=10)issues.push({query:q,pages:ranked.slice(0,5),recommendation:'consolidate-or-differentiate'});}return{ok:true,issues};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
