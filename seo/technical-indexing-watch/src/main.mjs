export function evaluate({urls=[]}){const issues=[];for(const u of urls){const id=u.url||u.name||'unknown';if(Number(u.httpStatus||200)>=400)issues.push({url:id,type:'http',severity:'critical',detail:`HTTP ${u.httpStatus}`});if(u.indexed===false)issues.push({url:id,type:'indexing',severity:'high',detail:'not indexed'});if(u.noindex===true)issues.push({url:id,type:'noindex',severity:'high',detail:'noindex present'});if(u.robotsBlocked===true)issues.push({url:id,type:'robots',severity:'high',detail:'robots blocked'});if(u.userCanonical&&u.googleCanonical&&u.userCanonical!==u.googleCanonical)issues.push({url:id,type:'canonical',severity:'medium',detail:'Google canonical differs'});if(u.inSitemap===false)issues.push({url:id,type:'sitemap',severity:'low',detail:'missing from sitemap'});}return{ok:true,alert:issues.length>0,issues,critical:issues.filter(x=>['critical','high'].includes(x.severity)).length};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
