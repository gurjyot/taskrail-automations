export function evaluate({rows=[],minImpressions=100,minPosition=4,maxPosition=20,targetCtr=.04}){const opportunities=rows.map(r=>{const impressions=Number(r.impressions||0),clicks=Number(r.clicks||0),position=Number(r.position||99),ctr=Number(r.ctr??(impressions?clicks/impressions:0));const gap=Math.max(0,targetCtr-ctr);const score=impressions*gap*Math.max(0,(maxPosition-position+1)/(maxPosition-minPosition+1));return{...r,impressions,clicks,position,ctr,score};}).filter(r=>r.impressions>=minImpressions&&r.position>=minPosition&&r.position<=maxPosition&&r.ctr<targetCtr).sort((a,b)=>b.score-a.score);return{ok:true,count:opportunities.length,opportunities};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
