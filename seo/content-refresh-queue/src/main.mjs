export function evaluate({pages=[]}){const queue=pages.map(p=>{const old=Number(p.previousClicks||0),now=Number(p.currentClicks||0),decline=old?Math.max(0,(old-now)/old):0;const impressions=Number(p.currentImpressions||0);const age=Math.min(1,Number(p.ageDays||0)/365);const position=Number(p.position||50);const rankability=position<=20?1:position<=40?.5:.1;const score=decline*2+Math.log10(impressions+1)*.3+age*.5+rankability;return{...p,decline,score,recommendation:decline>.25?'refresh-now':age>.75?'review-freshness':'monitor'};}).filter(p=>p.recommendation!=='monitor').sort((a,b)=>b.score-a.score);return{ok:true,queue};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
