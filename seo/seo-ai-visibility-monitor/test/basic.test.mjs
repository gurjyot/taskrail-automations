import test from 'node:test';import assert from 'node:assert/strict';import {analyze} from '../src/main.mjs';test('flags material search/local drops and low AI visibility',()=>{const r=analyze({search:{clicks:70,impressions:700,ctr:.1,position:8},previousSearch:{clicks:100,impressions:1000,ctr:.1,position:8},local:{impressions:70,websiteClicks:10,calls:3,directions:2},previousLocal:{impressions:100,websiteClicks:10,calls:3,directions:2},technical:{ok:true},aiReferences:[{mentioned:false},{mentioned:false},{mentioned:false},{mentioned:false},{mentioned:true,cited:false}]});assert.equal(r.issues.some(x=>x.code==='impressions_down'),true);assert.equal(r.issues.some(x=>x.code==='gbp_impressions_down'),true);assert.equal(r.issues.some(x=>x.code==='low_ai_visibility'),false)});test('critical technical outage outranks optimization work',()=>{const r=analyze({technical:{ok:false}});assert.equal(r.issues[0].severity,'critical');assert.match(r.actions[0],/availability\/crawlability/)});
