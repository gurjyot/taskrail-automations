# Agent instructions

Search the catalog before creating an automation. Keep workflows thin. Search TaskRail Capabilities before writing integration logic. If reusable logic is needed, promote it to a capability. Never add secrets. Preserve isolation. Run `npm run check && npm test`.
