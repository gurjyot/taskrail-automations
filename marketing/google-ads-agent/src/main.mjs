const r=(a,b)=>b?Number(a)/Number(b):1;export function evaluate({current,previous}){if(!current||!previous)throw new Error('current and previous required');const spend=r(current.spend,previous.spend),conv=r(current.conversions,previous.conversions),cpa=r(current.cpa,previous.cpa),roas=r(current.roas,previous.roas),ctr=r(current.ctr,previous.ctr);let decision='KEEP',reason='stable';if(spend>1.15&&conv>=1&&cpa<=1.05&&(current.roas==null||roas>=.95)){decision='SCALE';reason='scale is holding efficiency'}else if(conv<.85&&(cpa>1.15||roas<.85)){decision='REDUCE';reason='conversion efficiency deteriorated'}else if(ctr<.85&&cpa>1.1){decision='INVESTIGATE';reason='traffic quality weakened'}else if(Math.abs(spend-1)>.1||Math.abs(conv-1)>.1){decision='WATCH';reason='meaningful movement without clear failure'}return{ok:true,decision,reason,ratios:{spend,conversions:conv,cpa,roas,ctr},mutationAllowed:false};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
