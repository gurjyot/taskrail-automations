const ratio=(a,b)=>b?Number(a)/Number(b):1;export function evaluate({current,previous,ageDays=0,thresholds={}}){if(!current||!previous)throw new Error('current and previous required');let score=0;const reasons=[];if(Number(current.frequency)>=Number(thresholds.frequency??3)){score+=2;reasons.push('high frequency')}if(ratio(current.ctr,previous.ctr)<Number(thresholds.ctrRatio??0.85)){score+=2;reasons.push('CTR deteriorated')}if(ratio(current.cpc,previous.cpc)>Number(thresholds.cpcRatio??1.2)){score+=1;reasons.push('CPC increased')}if(current.cpa&&previous.cpa&&ratio(current.cpa,previous.cpa)>1.2){score+=2;reasons.push('CPA worsened')}if(Number(ageDays)>=Number(thresholds.ageDays??21)){score+=1;reasons.push('creative age')}return{ok:true,score,severity:score>=5?'high':score>=3?'medium':score?'low':'none',fatigued:score>=3,creativeRequest:score>=3,reasons};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
