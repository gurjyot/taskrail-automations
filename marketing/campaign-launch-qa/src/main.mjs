export function evaluate(c={}){const issues=[];if(!(Number(c.budget)>0))issues.push('missing or invalid budget');if(!c.destinationUrl)issues.push('missing destination URL');else{try{new URL(c.destinationUrl)}catch{issues.push('invalid destination URL')}}if(c.requireUtm!==false&&c.destinationUrl&&!/[?&]utm_(source|campaign)=/i.test(c.destinationUrl))issues.push('UTM tracking missing');if(!c.conversionEvent)issues.push('conversion event missing');if(!c.targeting||Object.keys(c.targeting).length===0)issues.push('targeting missing');if(!Array.isArray(c.creatives)||c.creatives.length===0)issues.push('creative missing');else if(c.creatives.some(x=>!x?.headline&&!x?.asset))issues.push('incomplete creative');return{ok:true,verdict:issues.length?'BLOCKED':'PASS',deployAllowed:issues.length===0,issues};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
