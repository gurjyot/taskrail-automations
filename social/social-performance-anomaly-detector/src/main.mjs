export function evaluate({current={},baseline={},tolerance=.3}={}){const findings=[];for(const k of Object.keys(current)){const b=Number(baseline[k]);const c=Number(current[k]);if(Number.isFinite(b)&&b!==0&&Number.isFinite(c)&&Math.abs((c-b)/b)>=tolerance)findings.push({metric:k,current:c,baseline:b,change:(c-b)/b})}return{ok:true,alert:findings.length>0,findings};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}