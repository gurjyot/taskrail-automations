export function evaluate({posts=[],now=Date.now()}={}){const queue=posts.filter(p=>p.status==='pending-approval').map(p=>({...p,overdue:Boolean(p.approvalDueAt&&Date.parse(p.approvalDueAt)<now)})).sort((a,b)=>Number(b.overdue)-Number(a.overdue));return{ok:true,queue,count:queue.length,alert:queue.some(x=>x.overdue)};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}