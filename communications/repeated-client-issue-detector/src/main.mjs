export function evaluate({issues=[],minimumRepeats=3}={}){const counts=new Map;for(const i of issues){const k=String(i.issueKey||i.category||'unknown');counts.set(k,(counts.get(k)||0)+1)}const repeated=[...counts].filter(([,count])=>count>=minimumRepeats).map(([issueKey,count])=>({issueKey,count}));return{ok:true,alert:repeated.length>0,repeated};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}