export function evaluate({messages=[],criticalTerms=['down','payment failed','cannot login','urgent']}={}){const results=messages.map(m=>{const t=String(m.text||'').toLowerCase();const hits=criticalTerms.filter(x=>t.includes(x));return{...m,priority:hits.length?'urgent':'normal',reasons:hits}});return{ok:true,results,urgent:results.filter(r=>r.priority==='urgent')};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}