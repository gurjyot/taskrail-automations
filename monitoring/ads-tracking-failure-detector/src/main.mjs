export function evaluate({current,baseline,minSpend=1,minClicks=10}){if(!current||!baseline)throw new Error('current and baseline required');const spend=Number(current.spend||0),clicks=Number(current.clicks||0),conv=Number(current.conversions||0),baseRate=Number(baseline.conversionRate||0),baseConv=Number(baseline.conversions||0);const expected=clicks*baseRate;const signalGone=spend>=minSpend&&clicks>=minClicks&&conv===0&&(baseConv>0||expected>=1);const severeDrop=expected>=2&&conv/expected<0.2;const trafficHealthy=Number(current.ctr||0)>=Number(baseline.ctr||0)*0.7;const likelyTracking=trafficHealthy&&(signalGone||severeDrop);return{ok:true,alert:likelyTracking,status:likelyTracking?'tracking-investigation':'normal-performance-review',expectedConversions:expected,reasons:[signalGone?'conversion signal disappeared':'',severeDrop?'conversions far below expected':'',trafficHealthy?'traffic still present':''].filter(Boolean)};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
