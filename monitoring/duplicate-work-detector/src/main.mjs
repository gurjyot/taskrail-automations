export function evaluate({items=[]}={}){const m=new Map;for(const i of items){const k=String(i.key||i.fingerprint||i.title||'').trim().toLowerCase();if(!k)continue;(m.get(k)||m.set(k,[]).get(k)).push(i)}const duplicates=[...m].filter(([,v])=>v.length>1).map(([key,items])=>({key,items}));return{ok:true,alert:duplicates.length>0,duplicates};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}