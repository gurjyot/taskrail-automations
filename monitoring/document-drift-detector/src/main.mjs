export function evaluate({documented={},observed={}}={}){const keys=new Set([...Object.keys(documented),...Object.keys(observed)]);const drift=[...keys].filter(k=>JSON.stringify(documented[k])!==JSON.stringify(observed[k])).map(k=>({key:k,documented:documented[k],observed:observed[k]}));return{ok:true,alert:drift.length>0,drift};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}