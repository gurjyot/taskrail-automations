# website-uptime

Reference thin workflow: receives a configured target, calls the governed `http-health-check` capability, and returns/report its status. The automation does not implement HTTP retry, timeout, TLS or classification logic itself.
