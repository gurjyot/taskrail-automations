import test from'node:test';import assert from'node:assert/strict';import{evaluate}from'../src/main.mjs';test('flags bad checksum',()=>assert.equal(evaluate({backups:[{status:'success',completedAt:new Date().toISOString(),checksumValid:false}]}).alert,true))