export function evaluate({leads=[],spendBySource={}}){const groups={};for(const l of leads){const k=l.sourceId||l.source||'unknown';const g=groups[k]??={total:0,qualified:0,won:0,junk:0};g.total++;if(l.qualified===true||['qualified','won'].includes(l.status))g.qualified++;if(l.status==='won')g.won++;if(l.status==='junk')g.junk++;}const results=Object.entries(groups).map(([sourceId,g])=>{const spend=Number(spendBySource[sourceId]||0);return{sourceId,...g,qualityRate:g.total?g.qualified/g.total:0,winRate:g.total?g.won/g.total:0,junkRate:g.total?g.junk/g.total:0,costPerQualified:g.qualified?spend/g.qualified:null};}).sort((a,b)=>b.qualityRate-a.qualityRate);return{ok:true,results};}export async function run(i={}){return evaluate(i)}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}
