# Follow-up Reminder

Generic CRM workflow: select due follow-ups, keep only actionable records, and produce a concise reminder payload. CRM access and delivery are delegated to `twenty-crm` and `telegram-bot`.
