export function evaluate({current={},baseline={},tolerance=0.3}={}){const findings=[];for(const k of ['orders','revenue']){const b=Number(baseline[k]||0),c=Number(current[k]||0);if(b>0&&Math.abs((c-b)/b)>=tolerance)findings.push({metric:k,current:c,baseline:b,change:(c-b)/b})}return{ok:true,alert:findings.length>0,findings};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}