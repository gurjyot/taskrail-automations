export function evaluate({current=[],previous=[],tolerance=0.05}={}){const p=new Map(previous.map(x=>[String(x.id),Number(x.price)]));const changes=current.filter(x=>p.has(String(x.id))&&p.get(String(x.id))>0).map(x=>({...x,change:(Number(x.price)-p.get(String(x.id)))/p.get(String(x.id))})).filter(x=>Math.abs(x.change)>=tolerance);return{ok:true,alert:changes.length>0,changes};}if(process.argv.includes('--health')){console.log('ok');process.exit(0)}